import * as React from "react";
import { useEffect, useState } from "react";

export default function Prompt({ socket, username, room }) {
  
  return (
    <div className = "prompt">

    </div>
  );
}